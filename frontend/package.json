{
  "name": "ai-chat-assistant-frontend",
  "version": "1.0.0",
  "description": "Next-gen AI Chat Assistant with TypeScript",
  "main": "index.html",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "serve": "vite preview --port 3000"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.9.0",
    "vite": "^4.5.0"
  },
  "dependencies": {
    "three": "^0.158.0",
    "@types/three": "^0.158.0",
    "marked": "^5.1.1",
    "highlight.js": "^11.8.0"
  }
}
